import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    flex: 1,
    backgroundColor: '#fff',
    left: 0,
    top: 0,
    right: 0,
    bottom: 0
  }
});